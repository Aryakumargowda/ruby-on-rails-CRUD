<%=stylesheet_link_tag('input')%>

<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New product</h1>

  <%= render "form", product: @product %>

  <%= link_to "Back", products_path, class: "back" %>
</div>
