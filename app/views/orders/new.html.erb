<%=stylesheet_link_tag('input')%>

<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New order</h1>

  <%= render "form", order: @order %>

  <%= link_to "Back", orders_path, class: "back" %>
</div>
