<%=stylesheet_link_tag('product')%>

<div class="notice">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Orders</h1>
  </div>
    <div id="products" class="min-w-full">
    <%= link_to "New Order", new_order_path, class: "new" %>
    <div class="tables">
      <table>
        <tr>
          <th>Ord.id</th>
          <th>Cust.id</th>
          <th>Order Date</th>
          <th>status</th>
          <th>Update</th>
          <th>Delete</th>
        </tr>

        <%= render @orders %>
      </table>
    </div>
  </div>
</div>
